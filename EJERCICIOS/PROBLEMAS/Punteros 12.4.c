#include <stdio.h>
#include <stdlib.h>

/*
    Se quiere evaluar las funciones f(x), g(x) y z(x) para todos los valores de
    x en el intervalo 0 <= x <3.5 con incremento de 0.2. Escribir un programa que
    evalue dichas funciones, utilizar un array de punteros a función.

    Las funciones son las siguientes:
    f(x) = 3*e-2x
    g(x) = -x*sin(x) + 1.5
    z(x) = x^2 - 2x + 3
*/

int main()
{

    return 0;
}